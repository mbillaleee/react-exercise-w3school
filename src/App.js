import logo from './logo.svg';
import './App.css';
import Car from './Components/Car';

function App() {
  const x = 11;
  return (
    <div className="App">
      <Car/>
      const myElement = <h1>{(x) < 10 ? "Hello" : "Goodbye"}</h1>;
    </div>
  );
}

export default App;
